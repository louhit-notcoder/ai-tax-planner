"""Production domain layer for Green Papaya.

This package deliberately keeps tax arithmetic, evidence, permissions, and AI tools
separate from the HTTP layer. The legacy API imports these modules while the product
is migrated incrementally.
"""

# Archived V2 reference only

These files are retained solely to assist migration and historical comparison. They are not imported by the V3 application, are not built by Docker, and must not be deployed. The active entry point is `backend/main.py`, which imports `app.main:app` and uses PostgreSQL.
